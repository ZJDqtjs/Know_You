# houduan_node 数据库表结构汇总

数据来源：`houduan_node/db/schema.sql`、`houduan_node/db/recommendation_profile.sql`

## 1. users（用户账号）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 用户主键 ID |
| username | VARCHAR(64) | NOT NULL, UNIQUE | 用户登录名 |
| password_hash | VARCHAR(255) | NOT NULL | 密码哈希值 |
| nickname | VARCHAR(64) | 可空 | 用户昵称 |
| avatar | VARCHAR(255) | 可空 | 头像 URL |
| gender | ENUM('male','female','other') | 可空 | 性别 |
| birthday | DATE | 可空 | 生日 |
| role | ENUM('guardian','dependent','normal') | DEFAULT 'normal' | 角色（监护人/被监护人/普通用户） |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP, ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

## 2. refresh_tokens（刷新令牌）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 刷新令牌主键 ID |
| user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 关联用户 ID |
| token | VARCHAR(255) | NOT NULL, UNIQUE | 刷新令牌值 |
| revoked | TINYINT(1) | DEFAULT 0 | 是否吊销（0 否，1 是） |
| expires_at | DATETIME | NOT NULL | 过期时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

## 3. bindings（绑定关系）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 绑定关系主键 ID |
| owner_user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 发起绑定用户 ID |
| target_user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 目标用户 ID |
| status | ENUM('pending','accepted','rejected') | DEFAULT 'pending' | 绑定状态 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

补充约束：UNIQUE KEY `uniq_pair` (owner_user_id, target_user_id)

## 4. link_codes（绑定码）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 绑定码主键 ID |
| code | VARCHAR(16) | NOT NULL, UNIQUE | 绑定码 |
| owner_user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 绑定码所属用户 ID |
| expired_at | DATETIME | NOT NULL | 过期时间 |
| used | TINYINT(1) | DEFAULT 0 | 是否已使用（0 否，1 是） |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

## 5. health_data（健康数据快照）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 健康数据主键 ID |
| user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 用户 ID |
| heart_rate | INT | 可空 | 心率 |
| steps | INT | 可空 | 步数 |
| systolic | INT | 可空 | 收缩压 |
| diastolic | INT | 可空 | 舒张压 |
| sleep_hours | DECIMAL(4,2) | 可空 | 睡眠时长（小时） |
| collected_at | DATETIME | NOT NULL | 数据采集时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 入库时间 |

补充约束：INDEX `idx_user_time` (user_id, collected_at)

## 6. weather（天气信息）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 天气记录主键 ID |
| user_id | BIGINT | NOT NULL, UNIQUE, FOREIGN KEY -> users(id), ON DELETE CASCADE | 用户 ID（每用户一条） |
| city | VARCHAR(64) | 可空 | 城市名 |
| lat | DECIMAL(9,6) | 可空 | 纬度 |
| lon | DECIMAL(9,6) | 可空 | 经度 |
| temperature | DECIMAL(5,2) | 可空 | 温度 |
| humidity | DECIMAL(5,2) | 可空 | 湿度 |
| condition | VARCHAR(64) | 可空 | 天气状况 |
| updated_at | DATETIME | NOT NULL | 天气更新时间 |

## 7. devices（设备）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 设备主键 ID |
| user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 所属用户 ID |
| name | VARCHAR(128) | NOT NULL | 设备名称 |
| type | ENUM('LIGHT','AC','COOKER','PHONE_SCREEN','OTHER') | DEFAULT 'OTHER' | 设备类型 |
| state | ENUM('on','off') | DEFAULT 'off' | 设备开关状态 |
| battery | INT | 可空 | 电量 |
| temperature | DECIMAL(5,2) | 可空 | 设备温度 |
| last_updated | DATETIME | NOT NULL | 设备状态更新时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

## 8. schedules（日程）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 日程主键 ID |
| user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 用户 ID |
| title | VARCHAR(128) | NOT NULL | 日程标题 |
| time | DATETIME | NOT NULL | 日程时间 |
| repeat_rule | ENUM('NONE','DAILY','WEEKLY') | DEFAULT 'NONE' | 重复规则 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

## 9. screen_sessions（屏幕远控会话）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 会话主键 ID |
| session_id | VARCHAR(64) | NOT NULL, UNIQUE | 会话业务 ID |
| controller_user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 控制端用户 ID |
| target_user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 被控端用户 ID |
| status | ENUM('pending','active','rejected','closed') | DEFAULT 'pending' | 会话状态 |
| target_ip | VARCHAR(45) | 可空, COMMENT 'IPv4或IPv6地址' | 被控端 IP |
| accepted_at | TIMESTAMP | 可空 | 接受时间 |
| closed_at | TIMESTAMP | 可空 | 关闭时间 |
| closed_by | ENUM('initiator','target','system') | 可空 | 关闭方 |
| duration_seconds | INT | DEFAULT 0 | 会话时长（秒） |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP, ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

## 10. session_commands（会话控制指令）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 指令主键 ID |
| session_id | BIGINT | NOT NULL, FOREIGN KEY -> screen_sessions(id), ON DELETE CASCADE | 所属会话 ID |
| command_id | VARCHAR(64) | NOT NULL, UNIQUE | 指令业务 ID |
| action | VARCHAR(32) | NOT NULL | 指令动作 |
| payload | JSON | 可空 | 指令载荷 |
| status | ENUM('sent','executed','failed') | DEFAULT 'sent' | 指令状态 |
| executed_at | TIMESTAMP | 可空 | 执行时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

## 11. control_commands（控制命令）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 控制命令主键 ID |
| command_uuid | VARCHAR(64) | NOT NULL, UNIQUE | 命令唯一标识 |
| session_id | BIGINT | 可空, FOREIGN KEY -> screen_sessions(id), ON DELETE SET NULL | 关联会话 ID |
| target_type | ENUM('user','device') | NOT NULL | 控制目标类型 |
| target_id | BIGINT | NOT NULL | 控制目标 ID |
| command | VARCHAR(64) | NOT NULL | 命令名称 |
| payload | JSON | 可空 | 命令参数 |
| status | ENUM('queued','sent','executed','failed') | DEFAULT 'queued' | 命令状态 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

## 12. logs_control（控制操作日志）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 日志主键 ID |
| command_id | BIGINT | NOT NULL, FOREIGN KEY -> control_commands(id), ON DELETE CASCADE | 控制命令 ID |
| command | VARCHAR(64) | NOT NULL | 命令名称快照 |
| from_user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 操作发起人 ID |
| target_user_id | BIGINT | 可空, FOREIGN KEY -> users(id), ON DELETE SET NULL | 目标用户 ID |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 记录时间 |

## 13. logs_health_access（健康访问日志）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 日志主键 ID |
| accessed_by | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 访问者用户 ID |
| user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 被访问健康数据所属用户 ID |
| accessed_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 访问时间 |

## 14. community_posts（社区帖子）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 帖子主键 ID |
| user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 发帖用户 ID |
| title | VARCHAR(200) | 可空 | 帖子标题 |
| content | TEXT | 可空 | 帖子文本内容 |
| voice_url | VARCHAR(255) | 可空 | 语音地址 |
| voice_duration | INT | 可空 | 语音时长（秒） |
| image_urls | JSON | 可空 | 图片 URL 列表 |
| like_count | INT | DEFAULT 0 | 点赞数 |
| comment_count | INT | DEFAULT 0 | 评论数 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP, ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

补充约束：INDEX `idx_user_time` (user_id, created_at)

## 15. community_comments（社区评论）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 评论主键 ID |
| post_id | BIGINT | NOT NULL, FOREIGN KEY -> community_posts(id), ON DELETE CASCADE | 所属帖子 ID |
| user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 评论用户 ID |
| content | TEXT | 可空 | 评论文本 |
| voice_url | VARCHAR(255) | 可空 | 语音地址 |
| voice_duration | INT | 可空 | 语音时长（秒） |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

补充约束：INDEX `idx_post_time` (post_id, created_at)

## 16. community_post_likes（社区点赞）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 点赞主键 ID |
| post_id | BIGINT | NOT NULL, FOREIGN KEY -> community_posts(id), ON DELETE CASCADE | 帖子 ID |
| user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 点赞用户 ID |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 点赞时间 |

补充约束：UNIQUE KEY `uniq_post_user` (post_id, user_id)

## 17. mall_categories（商城分类）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 分类主键 ID |
| name | VARCHAR(64) | NOT NULL | 分类名称 |
| icon | VARCHAR(255) | 可空 | 分类图标 URL |
| sort_order | INT | DEFAULT 0 | 排序值 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

## 18. mall_products（商城商品）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 商品主键 ID |
| category_id | BIGINT | 可空, FOREIGN KEY -> mall_categories(id), ON DELETE SET NULL | 分类 ID |
| name | VARCHAR(128) | NOT NULL | 商品名称 |
| price | DECIMAL(10,2) | NOT NULL | 商品价格 |
| sold_count | INT | DEFAULT 0 | 销量 |
| image_url | VARCHAR(255) | 可空 | 商品图片 URL |
| desc | TEXT | 可空 | 商品描述 |
| stock | INT | DEFAULT 0 | 库存 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

## 19. mall_orders（商城订单）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 订单主键 ID |
| user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE | 下单用户 ID |
| status | ENUM('pending','paid','shipped','completed','cancelled') | DEFAULT 'pending' | 订单状态 |
| total_price | DECIMAL(10,2) | NOT NULL | 订单总价 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

## 20. mall_order_items（商城订单明细）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 明细主键 ID |
| order_id | BIGINT | NOT NULL, FOREIGN KEY -> mall_orders(id), ON DELETE CASCADE | 所属订单 ID |
| product_id | BIGINT | 可空, FOREIGN KEY -> mall_products(id), ON DELETE SET NULL | 商品 ID |
| name | VARCHAR(128) | NOT NULL | 下单时商品名快照 |
| price | DECIMAL(10,2) | NOT NULL | 下单时单价快照 |
| quantity | INT | DEFAULT 1 | 购买数量 |
| image_url | VARCHAR(255) | 可空 | 下单时商品图快照 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

## 21. user_profiles（推荐用户画像）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT, NOT NULL | 用户画像主键 ID |
| user_id | BIGINT | NOT NULL, UNIQUE, FOREIGN KEY -> users(id), ON DELETE CASCADE, ON UPDATE CASCADE | 用户 ID（每用户一条画像） |
| tags | JSON | 可空 | 标签集合 |
| keywordWeights | JSON | 可空 | 关键词权重 |
| postVector | JSON | 可空 | 帖子侧向量 |
| productVector | JSON | 可空 | 商品侧向量 |
| stats | JSON | 可空 | 统计信息 |
| last_rebuild_at | DATETIME | 可空 | 最近重建时间 |
| createdAt | DATETIME | NOT NULL | 创建时间 |
| updatedAt | DATETIME | NOT NULL | 更新时间 |

## 22. user_behavior_events（用户行为事件）

| 字段名 | 数据类型 | 约束 | 描述 |
| --- | --- | --- | --- |
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT, NOT NULL | 行为事件主键 ID |
| user_id | BIGINT | NOT NULL, FOREIGN KEY -> users(id), ON DELETE CASCADE, ON UPDATE CASCADE | 用户 ID |
| scene | ENUM('community','mall','search','system') | NOT NULL | 行为场景 |
| action | VARCHAR(32) | NOT NULL | 行为动作 |
| targetType | ENUM('post','product','keyword','other') | NOT NULL, DEFAULT 'other' | 目标类型 |
| targetId | BIGINT | 可空 | 目标对象 ID |
| payload | JSON | 可空 | 事件扩展数据 |
| createdAt | DATETIME | NOT NULL | 创建时间 |
| updatedAt | DATETIME | NOT NULL | 更新时间 |

补充约束：
- KEY `idx_user_behavior_events_user_id_createdAt` (user_id, createdAt)
- KEY `idx_user_behavior_events_scene_action` (scene, action)

---

## 备注

1. 本文档描述的是建表 SQL 中定义的逻辑约束（PK/UK/FK/默认值/索引）。
2. 若线上库有额外迁移或手动 DDL 变更，应以线上实际 `SHOW CREATE TABLE` 结果为准。